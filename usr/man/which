NAME
  which - locate a command

SYNOPSIS
  which COMMAND [...]

DESCRIPTION
  This program writes the full path to each of the specified programs to the standard output. If a program is an alias, this is indicated.

EXAMPLES
  which ls
    Displays `/bin/ls.lua`.

  which doesntexist dir
    Displays nothing.
